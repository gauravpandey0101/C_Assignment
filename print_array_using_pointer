#include<stdio.h>
int main()
{
    int a[10],i,*p;
    p = &a[0];
    
    for(i=0;i<5;i++)
    {
        scanf("%d",&a[i]);
    }
    
    printf("Display Elements\n");
    for(i=0;i<5;i++)
    {
     printf("a[%d] = %d\n",i,*p++);   
    }
    return 0;
}
