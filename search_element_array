#include<stdio.h>
int main()
{
    int a[10],sum=0,i,j,temp,num,count=0;
    for(i=0;i<5;i++)
    {
        scanf("%d",&a[i]);
    }
    printf("Input for search element\n");
    scanf("%d",&num);
    for(i=1;i<5;i++)
    {
        if(num == a[i])
        {
            printf("Found element\n");
            count++;
            break;
        }
    }
    if(count == 0)
    {
        printf("Not Found Element");
    }
    else
    {
        printf("Found Element");
    }
    return 0;
}
